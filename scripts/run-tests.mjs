/**
 * Legacy entry kept for backwards compatibility. The canonical test runner now
 * lives in tests/run-tests.mjs so that additional unit suites can run alongside
 * the original layout harness.
 */
import "../tests/run-tests.mjs";
